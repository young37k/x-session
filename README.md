# Archery App Full

## 실행 방법

1. 압축 해제
2. 터미널에서 프로젝트 폴더로 이동
3. 아래 명령 실행

```bash
npm install
```

`.env.example`를 복사해서 `.env` 파일을 만들고 Firebase 값을 입력한 뒤:

```bash
npm run dev
```

브라우저에서 `http://localhost:5173` 접속
