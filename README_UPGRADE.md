# 듀얼 입력 업그레이드

이번 버전에는 다음이 추가되었습니다.

- 입력 방식 선택
  - 엔드 기반
  - 거리 기반
- 거리 기반 UI
- 거리 기록 추가/삭제
- 거리당 화살 수 입력
- Firebase 저장 구조에 `recordInputType`, `distanceRounds`, `arrowsPerDistance` 추가

실행:
1. 터미널에서 폴더 이동
2. npm install
3. .env.example 를 복사해 .env 생성 후 Firebase 값 입력
4. npm run dev
